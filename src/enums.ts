export enum ErrorLogs {
    FIELD_Y_MUST_BE_NUMBER = "The field Y must be number",
    Y_VALUE_VALIDATE_ERROR = "Y value should be from -3 to 3",
    X_MUST_BE_CHOSEN = "Value X must be chosen",
    R_MUST_BE_CHOSEN = "Value R must be chosen"
}
